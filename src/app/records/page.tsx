import TabsView from '@/components/RecordsScreen/TabsView';

const Records = () => {
  return (
    <div className={`flex flex-col h-full max-w-[1500px] p-0`}>
      <TabsView />
    </div>
  );
};

export default Records;
